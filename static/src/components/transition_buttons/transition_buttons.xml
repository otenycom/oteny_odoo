<?xml version="1.0" encoding="UTF-8"?>
<templates>
    <t t-name="riverflow.TransitionButtons">
        <t t-if="this.iconClass()">
            <span t-att-class="this.iconClass()"></span>
            <!-- workaround: &nbsp; does not work -->
            <span>&#160;</span>
        </t>
        <span t-esc="this.text()" />
        <div>
            <t t-set="buttons" t-value="this.buttonDefs()" />
            <t t-foreach="buttons" t-as="button" t-key="button.index">
                <button
                    t-att-name="button.action"
                    class="btn btn-primary transition_button"
                    t-on-click.stop="() => this.executeTransition(button)"
                    type="object">
                    <span t-esc="button.caption" />
                </button>
            </t>
        </div>
    </t>
</templates>