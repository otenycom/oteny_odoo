<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="riverflow.TimingWidget">
        <span>
            <span t-att-class="dateClass" t-att-title="date">
                <t t-esc="date"/>
            </span>

            <t t-if="relativedays">
                (<t t-esc="relativedays" t-att-title="relativedays"/>)
            </t>

            <!-- Too much info -->
            <!-- <t t-if="daysRemaining">
                <br/>in 
                <span t-att-class="dateClass" t-att-title="daysRemaining">
                    <t t-esc="daysRemaining"/>d
                </span>
            </t> -->
        </span>
    </t>
</templates>